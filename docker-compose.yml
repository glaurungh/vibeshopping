services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vibeshopping
      POSTGRES_PASSWORD: vibeshopping
      POSTGRES_DB: vibeshopping
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vibeshopping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
